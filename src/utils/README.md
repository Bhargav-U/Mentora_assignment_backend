# Utils

**Status**
Utilities are currently minimal. `apiError.js` and `apiResponse.js` exist as placeholders and are not used by the current code.

**When To Use**
Add shared, framework-agnostic helpers here as the codebase grows.
